eval_id: airflow_verification
task:
  name: "Airflow Requirement"
  description: >
    Given a heatsink and power dissipation, determine the required airflow (CFM)
    to maintain the component below 70°C. Provide reasoning and describe the
    assumptions used.

# Reference to the JSON file(s)
cases:
  - path: "cases_airflow_verification.json"

checks:
  - name: "no_hallucinated_table_values"
    type: "referential_integrity"
  - name: "temperature_margin_correct"
    type: "numerical_consistency"
  - name: "units_correct"
    type: "unit_validation"

metadata:
  engineering_skills:
    - convective cooling
    - interpreting lookup tables
    - numerical reasoning
